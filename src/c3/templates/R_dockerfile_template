FROM r-base:4.3.2
USER root
RUN apt update
${requirements_docker}
ADD ${target_code} /home/docker/
ADD ${additional_files_path} /home/docker/
RUN chmod -R 777 /home/docker/
USER docker
CMD ["${command}", "/home/docker/${target_code}"]