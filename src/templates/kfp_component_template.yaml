name: $name
description: $description

inputs:
$inputs

implementation:
    container:
        image: $container_uri:$version
        command:
        - sh
        - -ec
        - |
          python $call
$input_for_implementation